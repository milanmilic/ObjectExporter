<Window x:Class="ObjectExporter.Views.TimeoutDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ObjectExporter - Operation is taking longer"
        Height="220" Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="True"
        Topmost="True"
        Background="#F0F0F0">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Icon and message -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="⏳" FontSize="28" Margin="0,0,10,0" VerticalAlignment="Center" />
            <TextBlock x:Name="MessageText"
                       Text="The export operation is taking longer than expected..."
                       TextWrapping="Wrap"
                       FontSize="13"
                       VerticalAlignment="Center" />
        </StackPanel>

        <!-- Progress bar -->
        <ProgressBar Grid.Row="1"
                     IsIndeterminate="True"
                     Height="6"
                     Margin="0,0,0,6" />

        <!-- Progress text -->
        <TextBlock Grid.Row="2"
                   x:Name="ProgressText"
                   Text=""
                   FontSize="11"
                   Foreground="#666666"
                   Margin="0,0,0,12" />

        <!-- Buttons -->
        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Bottom">

            <Button x:Name="CancelButton"
                    Content="Cancel now"
                    Width="120" Height="30"
                    Margin="0,0,10,0"
                    Click="CancelButton_Click"
                    IsDefault="False" />

            <Button x:Name="WaitButton"
                    Content="Wait"
                    Width="120" Height="30"
                    Click="WaitButton_Click"
                    IsDefault="True" />
        </StackPanel>
    </Grid>
</Window>